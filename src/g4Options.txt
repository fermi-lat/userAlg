//##############################################################
//
// Job options file for G4Generator
// 
// $Header$

//  -------------- basic Glast/Gaudi run enviornment -----------
ApplicationMgr.DLLs   = { "GaudiAlg",  "GlastSvc"};

ApplicationMgr.ExtSvc = { 
    "GlastEventSelector/EventSelector" , 
    "EventCnvSvc", 
    "GlastDetSvc" 
    };

EventPersistencySvc.CnvServices = {"EventCnvSvc"};
EventSelector.Input = "NONE";
// make sure that this service does not try to instantatiate the (gismo) detector
GlastDetSvc.PersistencyFile="(none)";

// Stuff for detModel
// The top volume used for this run
GlastDetSvc.topVolume="LAT";


#include "$FLUXSVCROOT/src/defaultOptions.txt"
// Append (no replace) the list of source library documents
FluxSvc.source_lib = {"$(G4GENERATORROOT)/src/test/test_sources.xml"};

//----------------- local algorithms to run -------------------
ApplicationMgr.TopAlg = { 
    "G4Generator"};

// Set output level threshold (2=DEBUG, 3=INFO, 4=WARNING, 5=ERROR, 6=FATAL )
MessageSvc.OutputLevel      = 2;


// ask for a source that is in the library
//G4Generator.source_name="gamma_patch_for_G4";
G4Generator.source_name="muon_pencil";
//G4Generator.source_name="muon_patch";
//G4Generator.source_name="gamma_test";

// commands to run before each event. 
//G4Generator.UIcommands+={"/run/verbose 3"};
//G4Generator.UIcommands+={"/event/verbose 3"};
G4Generator.UIcommands+={"/tracking/storeTrajectory 1"};
//G4Generator.UIcommands+={"/tracking/verbose 0"};

ApplicationMgr.EvtMax =100;

// needed to have the gui available
ApplicationMgr.ExtSvc +={"GuiSvc"};
GuiSvc.size = -2000; 

//==============================================================
//
// End of job options file
//
//##############################################################

